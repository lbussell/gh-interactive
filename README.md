# gh-interactive
GitHub CLI extension that is entirely TUI driven

## Local installation

Build the extension binary and install the current checkout with the GitHub CLI:

```sh
just build install
```

Run the extension with:

```sh
gh interactive
```
